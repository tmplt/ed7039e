package dwm;

// TODO: refer to DWM documentation
struct position_t
{
        int64_t timestamp; // in milliseconds
        int32_t x;
        int32_t y;
        int32_t z;
        int8_t q;
}

struct acceleration_t
{
        int64_t timestamp; // in milliseconds
        int64_t x;
        int64_t y;
        int64_t z;
}